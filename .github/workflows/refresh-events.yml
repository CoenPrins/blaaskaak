name: Refresh Calendar

on:
  schedule:
    - cron: '42 4 * * *'

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Download ics file
      run: |
        echo hi
