name: Refresh Calendar

on:
  schedule:
    - cron: '32 15 * * *'

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Download ics file
      run: |
        echo hi
